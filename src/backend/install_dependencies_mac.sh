# Install dependencies for the backend
# Run './install_dependencies_mac.sh' from the backend directory to install dependencies
# FOR MAC USERS

pip install psycopg2
pip install python-dotenv
pip install pytest